CREATE DATABASE BIBLIOTECA;
USE BIBLIOTECA;

CREATE TABLE LIVRO (
   ID INT NOT NULL AUTO_INCREMENT,
   TITULO VARCHAR(50),
   AUTOR VARCHAR(50),
   ANO INT,
   PRIMARY KEY(ID)
);


INSERT INTO LIVRO (TITULO,AUTOR,ANO) VALUES ('DOM CASMURRO','MACHADO DE ASSIS',2022);
INSERT INTO LIVRO (TITULO,AUTOR,ANO) VALUES ('VIDAS SECAS','GRACILIANO RANOS',2020);
INSERT INTO LIVRO (TITULO,AUTOR,ANO) VALUES ('IRACEMA','JOSÉ ALENCAR',2021);

CREATE TABLE RESERVA (
   ID INT NOT NULL AUTO_INCREMENT,
   NOME_USUARIO VARCHAR(50),
   DATA_RESERVA DATE,
   LIVRO_ID INT,
   PRIMARY KEY(ID),
   FOREIGN KEY(LIVRO_ID) REFERENCES LIVRO(ID)  
);